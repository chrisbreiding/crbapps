// Generated by CoffeeScript 1.4.0
(function() {

  define(['calculation'], function(calculation) {
    var CalcOperator;
    return CalcOperator = (function() {

      function CalcOperator(value, type) {
        this.value = value;
        this.type = type != null ? type : 'op';
      }

      CalcOperator.prototype.process = function() {
        var cC, prevType, type, value;
        cC = calculation.collection;
        prevType = calculation.result.type;
        value = this.value;
        type = this.type;
        if (!cC.length) {
          cC.push({
            value: '0',
            type: 'num'
          });
        }
        if (prevType === type) {
          cC[cC.length - 1].value = value;
        } else {
          cC.push({
            value: value,
            type: type
          });
        }
        return calculation.setResult(value, type);
      };

      return CalcOperator;

    })();
  });

}).call(this);
